/**
 * Supabase client factory for Next.js.
 *
 * Phase 1: Stub only — no credentials are set.
 * Phase 7: Implement using @supabase/ssr for cookie-based SSR auth.
 *
 * Two clients will be needed:
 *   - Browser client (for client components)
 *   - Server client (for server components and route handlers, using cookies)
 *
 * The service role key is ONLY used server-side and is never exposed to the browser.
 */

// Placeholder — will be implemented in Phase 7
export const supabase = null;
