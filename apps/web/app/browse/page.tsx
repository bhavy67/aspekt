// Browse — Phase 5: full catalog grid with sort and filter (category, mood)
// Rendered SSR to support URL query params (?category=X&mood=Y&sort=Z)
export default function BrowsePage() {
  return (
    <main>
      <p>Browse</p>
    </main>
  );
}
