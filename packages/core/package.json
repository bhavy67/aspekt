{
  "name": "@aspekt/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared business logic for the ASPEKT platform — Fit Engine, coin math, validation, utilities",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aspekt/types": "workspace:*"
  },
  "devDependencies": {
    "@aspekt/config": "workspace:*",
    "typescript": "~5.9.3",
    "vitest": "^5.0.0"
  }
}
