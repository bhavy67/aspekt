/**
 * @aspekt/types
 *
 * Shared TypeScript types for the ASPEKT platform.
 * This package will be populated during Phase 2 (UX Architecture)
 * and Phase 7 (Backend) when data models are finalized.
 *
 * Eventual exports will include:
 * - Wallpaper types
 * - Collection types
 * - Category / Mood types
 * - User / Account types
 * - Coin / Transaction types
 * - Entitlement types
 * - API response types
 * - Supabase database-generated types
 */

export type AspektVersion = string;

export const ASPEKT_PACKAGE = '@aspekt/types' as const;
